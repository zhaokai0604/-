# 简析智评项目行动指南

本文件是本项目的协作与实现规则。处理本项目的任何需求时，先阅读并遵守本文件。

## 一、任务处理原则

用户提出的方案只是待评估建议，不是必须原样执行的实现指令。每次接到任务后，必须先：

1. 阅读相关代码、数据、配置、测试和现有文档，确认当前真实状态。
2. 判断用户方案是否会造成数据泄漏、过拟合、隐私风险、指标失真、答辩漏洞或落地风险。
3. 明确说明方案中正确的部分、存在的问题和关键假设。
4. 如果方案不适合当前项目，直接否定并给出更优替代方案，不为了迎合用户而照做。
5. 对于明确要求实施的任务，按评估后的最优方案修改代码或材料，并完成相应验证。
6. 最终说明实际修改内容、验证结果、剩余风险和不能下结论的部分。

不得在没有核对项目现状的情况下直接执行用户的技术判断，也不得把探索性实验结果包装成验收结论。

## 二、项目技术决策底线

- 当前优先采用冻结语义模型、提示词工程、岗位库检索、规则护栏和证据解释，不因展示需要强行微调模型。
- 小样本微调必须先经过严格的训练集、验证集、测试集隔离；同源数据拟合参数后不能再作为独立验收结果。
- 测试集不得放入 Prompt、Few-shot 示例、规则调参或阈值选择过程。
- 真实简历标注的主要用途是解析评测、边界发现、提示词示例筛选、阈值校准和可信度验证，不默认用于模型训练。
- 规则引擎负责学历、经验等硬约束、风险提示和解释；语义模型负责主排序。规则分数不能未经验证直接与语义分数加权。
- 任何指标都必须注明数据口径、样本数量、切分方式和是否为探索性结果。

## 三、标注与数据安全

- 原始真实简历只在本地处理，不进入 Git、Prompt、训练集或对外分享包。
- 对外协作包只能包含脱敏 TXT、匿名文件名、标注工具、教程和必要脚本。
- 真实姓名、电话、邮箱、身份证号、学校等敏感信息不得写入评测 JSON、报告或日志。
- 双人标注必须使用同一批样本、独立完成、结果分开保存，完成前不得互相查看标签。
- 两名标注员的结果必须使用稳定样本 ID 比对；分歧由负责人裁决并保留裁决记录。
- A 池用于清晰样本标注和解析评测，B 池用于复杂版式和能力边界，C 池只用于演示，不能混入训练或正式测试。
- 单人初标必须如实标记为单人初标，不得伪造双人一致性。

## 四、编码、编辑与验证

- 中文文件统一使用 UTF-8；修改后必须检查中文读取结果，禁止产生乱码。
- 优先沿用项目已有框架、接口、目录和测试模式，避免无关重构。
- 手工编辑文件使用 `apply_patch`；不要用 shell 重定向或脚本覆盖源码文件。
- 修改代码后按风险运行针对性测试；涉及接口、解析、匹配或共享逻辑时运行相关全量测试。
- 文档中的测试数字、模型指标、样本数量和功能描述必须与当前真实执行结果一致。
- 生成 DOCX/PDF 后必须检查中文、页码、表格、图示和占位文字。

## 五、答辩口径

项目强调可解释、可复现、可落地，不追求没有证据支撑的模型指标。对评委必须能够清楚回答：

- 哪些数据用于标注、训练、验证、测试和演示；
- 为什么测试集没有参与 Prompt 或参数选择；
- 解析失败时如何区分“简历质量差”和“机器没有读懂”；
- 规则、语义模型、岗位库和证据解释分别承担什么职责；
- 当前结果的适用范围和能力边界是什么。
