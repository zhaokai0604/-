# 演示说明

推荐演示流程：

1. 启动 Docker Compose 或本地脚本。
2. 打开前端页面。
3. 上传一份 `.docx` 简历，展示 DeepSeek 优先分析结果；如果未配置 API Key，则展示自动离线回退结果。
4. 输入目标岗位，展示岗位匹配度变化。
5. 下载 Word 和 PDF 报告。
6. 上传一个 `.zip`，展示批量分析结果。
7. 打开历史记录，展示查看和删除能力。

演示时默认优先使用 DeepSeek；即使没有网络或 DeepSeek API Key，系统也会自动回退到离线规则分析并完成核心流程。
