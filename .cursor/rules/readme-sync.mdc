---
description: 重大项目更新时必须同步维护根目录 README.md
alwaysApply: true
---

# README 同步更新

当本仓库发生**重大项目更新**时，必须在同一次改动中同步更新根目录 `README.md`，不要等用户再提醒。

## 什么算重大更新

- 核心能力增减或主链路变化（解析 / 匹配 / 评分 / 优化稿 / SSE / 教师端等）
- 对外演示路径、启动方式、默认账号或关键配置变化
- 评测/消融数字变化（解析 F1、匹配 Spearman、α 等）
- 新增或重命名重要文档、训练权重、评测脚本、附件说明
- 项目结构大幅调整（目录、一键启动脚本）

## 小改动可不同步

- 纯内部重构、单测修补、样式微调、文案错别字（若不影响评委使用说明）
- 临时调试脚本、本地 `.env`、不进入交付的草稿

## 更新时至少核对这些章节

1. 能力/亮点描述是否仍正确  
2. 「核心技术能力」中的数字与口径是否与最新评测一致  
3. 文档索引与项目结构树是否包含新文件  
4. 快速体验与演示路线是否仍可走通  

优先保持 README **评委可读、可启动、可答辩**；细节仍链到 `docs/`，避免把 README 写成论文。
