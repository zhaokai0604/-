# 简历评价智能体项目功能、技术选型与 AI 实现交接说明

## 0. 给接手 AI 的说明

这是一份可以直接交给另一个 AI 或开发者继续实现的项目交接文档。请把本文件作为项目的主要需求说明和实现依据。

项目从零开始开发，工程根目录固定为：

`C:\Users\Patiat\Documents\简历分析`

后续所有前端、后端、数据库脚本、启动脚本、Docker 配置、示例数据和说明文档都必须放在这个目录下。

实现时请遵守以下优先级：

1. 先完成“上传简历 -> 解析文本 -> 评分分析 -> 可视化展示 -> 导出报告 -> 保存历史记录”的核心闭环。
2. 先使用默认本地用户，不要一开始就做完整登录注册。
3. 默认优先使用 DeepSeek 分析，无法调用 DeepSeek 时自动回退到离线规则分析。
4. 系统必须保持完整离线兜底能力；无 DeepSeek API Key、无网络或 API 异常时仍可完成完整流程。
5. 所有上传文件、解压文件、报告文件都保存在本地项目目录中。
6. 登录功能放到最后，只在第一阶段预留数据库结构和接口位置。
7. 本项目目标是比赛可演示的完整原型，不是企业级全量系统。

## 1. 项目定位

本项目是面向高校学生的“简历评价智能体”系统，用于帮助学生上传简历后自动完成内容解析、模块识别、多维度评分、问题诊断、岗位匹配分析、修改建议生成和报告导出。

项目目标是参加数据素养类比赛，因此第一优先级是：

- 功能完整
- 演示清晰
- 可直接运行
- 可离线展示
- 数据安全说明充分
- 交付包结构清楚

## 2. 最小可运行版本目标

第一阶段必须实现一个可演示的最小完整版本。这个版本不要求登录注册完整上线，但必须能跑通真实业务流程。

最小版本必须支持：

- 启动 Vue 前端、FastAPI 后端和 MySQL 数据库。
- 上传单份 Word 简历并完成分析。
- 上传单份 PDF 简历并完成分析。
- 上传 ZIP 压缩包并批量分析其中的简历。
- 从简历中提取文本和核心模块。
- 在无网络、无 DeepSeek API Key 的情况下完成规则评分。
- 展示总分、分项评分、岗位匹配度、问题诊断和修改建议。
- 使用 ECharts 展示图表。
- 导出 Word 报告和 PDF 报告。
- 保存分析历史。
- 查看历史记录。
- 删除历史记录，并同步删除相关本地文件。

## 3. 核心功能说明

### 3.1 简历上传

系统支持单份简历上传和 ZIP 批量上传。

支持格式：

- Word 简历：`.docx`
- PDF 简历：`.pdf`
- 压缩包：`.zip`

`.doc` 老格式不作为第一阶段强制支持项。遇到 `.doc` 文件时，系统应给出清晰提示：请转换为 `.docx` 后重新上传。

上传时必须检查：

- 文件扩展名
- MIME 类型或文件签名
- 单文件大小
- ZIP 解压后总大小
- ZIP 内部文件路径是否合法

### 3.2 简历解析

系统需要从简历中提取文本内容，并识别关键模块。

主要模块包括：

- 个人基本信息
- 教育背景
- 实习经历
- 项目经历
- 校园经历
- 技能证书
- 获奖情况
- 自我评价或求职意向

解析输出应尽量结构化，供评分、岗位匹配、报告生成复用。

建议输出结构：

```json
{
  "raw_text": "完整简历文本",
  "sections": {
    "basic_info": [],
    "education": [],
    "internship": [],
    "projects": [],
    "campus": [],
    "skills": [],
    "awards": [],
    "summary": []
  },
  "detected_keywords": [],
  "warnings": []
}
```

### 3.3 OCR 扫描件识别

普通 PDF 先使用 `pdfplumber` 提取文本。如果提取文本为空或明显过短，则判断为可能的扫描件，并进入 OCR 流程。

OCR 使用 PaddleOCR，主要处理：

- 扫描版 PDF
- 图片型 PDF
- 无法直接提取文本的简历

OCR 失败时，系统不能崩溃，应返回可理解的错误信息，并保存失败状态到历史记录。

### 3.4 简历评分

系统采用规则评分为基础，DeepSeek 增强分析为可选能力。

评分维度：

- 内容完整性
- 经历匹配度
- 语言专业性
- 格式规范性
- 亮点突出度
- 岗位匹配度

每个维度建议使用 0-100 分。总分可以按加权平均计算。

建议默认权重：

- 内容完整性：20%
- 经历匹配度：20%
- 语言专业性：15%
- 格式规范性：15%
- 亮点突出度：15%
- 岗位匹配度：15%

规则评分必须离线可用。即使没有 API Key，也要能给出总分、分项分数、问题诊断和基础建议。

### 3.5 修改建议生成

系统需要给出具体、可执行的修改建议。

建议内容包括：

- 哪些模块缺失或不完整
- 哪些经历需要补充数据结果
- 哪些表述不够专业
- 哪些内容与目标岗位不匹配
- 可以如何改写简历中的表达
- 如何突出项目、实习或技能亮点

离线模式下，建议由规则模板生成。增强模式下，可在规则结果基础上调用 DeepSeek 生成更自然的建议。

### 3.6 岗位匹配分析

用户可以输入目标岗位名称或岗位 JD。系统根据岗位文本分析简历匹配度。

系统需要完成：

- 提取岗位关键词
- 提取岗位能力要求
- 对比简历中的经历、技能和项目
- 给出匹配度评分
- 指出缺失能力
- 指出冗余内容
- 给出优化方向

如果用户不输入岗位信息，系统仍然可以完成通用简历评分，但岗位匹配度维度应标记为“未提供目标岗位，使用通用求职匹配规则”。

### 3.7 可视化报告

前端需要展示清晰的分析结果。

可视化内容包括：

- 总评分
- 分项评分柱状图
- 能力维度雷达图
- 岗位匹配度
- 问题诊断列表
- 修改建议列表
- 批量简历对比结果

图表工具使用 ECharts。

### 3.8 报告导出

系统需要支持导出 Word 报告和 PDF 报告。

单份简历导出：

- Word 报告
- PDF 报告

ZIP 批量分析导出：

- 每份简历的单独 Word/PDF 报告
- 一份批量汇总报告

报告内容至少包括：

- 文件名
- 分析时间
- 目标岗位
- 总评分
- 分项评分
- 识别到的简历模块
- 问题诊断
- 修改建议
- 岗位匹配分析
- 数据安全说明或本地处理说明

### 3.9 历史记录

系统需要保存历史分析记录。

第一阶段不强制登录，使用默认用户 `default_user`。

所有分析记录必须带有 `user_id`，为后续登录功能做准备。

历史记录页面需要支持：

- 查看分析记录列表
- 查看单条分析详情
- 下载报告
- 删除记录

删除记录时必须同步删除：

- 原始上传文件
- ZIP 解压后的临时文件
- Word 报告
- PDF 报告

### 3.10 登录功能

登录功能放到最后实现，不作为第一阶段核心功能。

第一阶段只预留：

- `users` 表
- `analysis_records.user_id`
- 后端认证接口目录或路由占位
- 前端登录入口扩展空间

不要让登录功能阻塞核心简历分析功能。

### 3.11 ZIP 批量上传

ZIP 上传后，系统需要安全解压并逐份分析。

处理流程：

1. 上传 ZIP 文件。
2. 校验 ZIP 大小和类型。
3. 在隔离目录中安全解压。
4. 过滤出 `.docx` 和 `.pdf` 文件。
5. 逐份解析和评分。
6. 每份生成单独报告。
7. 生成批量汇总结果。
8. 页面展示批量分析列表。

ZIP 安全要求：

- 禁止路径穿越，例如 `../`。
- 禁止解压到项目目录之外。
- 限制压缩包大小。
- 限制解压后总大小。
- 忽略不支持文件类型。
- 对每个文件记录成功或失败状态。

### 3.12 数据安全

系统默认采用本地私有存储，同时默认优先使用 DeepSeek 分析。只有配置了 DeepSeek API Key 且前端开关开启时，才会把简历文本和规则评分结果发送到配置的 DeepSeek API。

数据安全要求：

- 上传文件保存在本地项目目录。
- 解压文件保存在隔离目录。
- 报告文件保存在本地报告目录。
- DeepSeek 分析默认开启，但未配置 API Key 或调用失败时自动回退离线规则分析。
- 用户在前端关闭 DeepSeek 时，系统完全离线运行。
- 用户可以删除历史记录。
- 删除历史记录时同步删除原始文件、临时文件和报告文件。
- `.env.example` 不包含真实密钥。
- 真实 `.env` 不应提交到版本管理。

## 4. 技术选型

### 4.1 前端：Vue

选择 Vue 作为前端框架。

主要用途：

- 构建上传页面
- 构建岗位输入页面
- 展示评分结果
- 展示可视化图表
- 展示历史记录
- 展示批量分析结果
- 提供报告下载入口

前端建议使用 Vue 3 + Vite。

### 4.2 后端：FastAPI

选择 FastAPI 作为后端框架。

主要用途：

- 提供文件上传接口
- 提供 ZIP 批量上传接口
- 调用简历解析模块
- 调用评分模块
- 调用岗位匹配模块
- 调用报告生成模块
- 管理历史记录
- 提供报告下载接口

后端接口统一使用 `/api` 前缀。

### 4.3 Word 解析：python-docx

使用 `python-docx` 解析 `.docx` 简历。

主要用途：

- 读取段落文本
- 读取表格文本
- 组合成完整简历文本

`.doc` 老格式第一阶段不做解析，只提示用户转换为 `.docx`。

### 4.4 PDF 解析：pdfplumber

使用 `pdfplumber` 解析普通 PDF。

主要用途：

- 提取 PDF 文本
- 提取表格附近文本
- 判断是否需要 OCR

### 4.5 OCR：PaddleOCR

使用 PaddleOCR 处理扫描件。

主要用途：

- 识别扫描版 PDF
- 识别图片型 PDF
- 提升中文简历解析覆盖率

注意：

- PaddleOCR 依赖较大。
- README 中必须写清安装方式。
- Docker 方案需要考虑模型缓存。

### 4.6 AI 能力：DeepSeek API + 规则兜底

DeepSeek API 是默认优先分析能力，但不是系统运行前提。

默认行为：

- 先使用本地规则完成解析、评分和岗位匹配。
- 默认尝试调用 DeepSeek 生成更自然、更具体的诊断和建议。

调用条件：

- 用户未关闭 DeepSeek 分析。
- `.env` 中配置了 DeepSeek API Key。
- 后端调用 DeepSeek 生成更自然的诊断和建议。

失败回退：

- API 超时、无 Key、无网络或返回错误时，系统自动回退到规则结果。
- 记录 `analysis_mode=offline_fallback`；用户主动关闭 DeepSeek 时记录 `analysis_mode=offline`。

### 4.7 数据处理：pandas + jieba + regex

使用 `pandas + jieba + regex` 处理文本和统计结果。

主要用途：

- 中文分词
- 关键词提取
- 模块识别
- 岗位匹配
- 批量结果汇总
- 图表数据整理

### 4.8 数据库：MySQL

使用 MySQL 保存业务数据。

部署策略：

- Docker Compose 内置 MySQL。
- 同时支持连接本机 MySQL。
- 通过 `.env` 切换数据库配置。

### 4.9 报告导出：python-docx + reportlab

使用 `python-docx` 生成 Word 报告。

使用 `reportlab` 生成 PDF 报告。

PDF 中文字体要求：

- 项目内放置中文字体文件。
- 建议目录：`assets/fonts/`
- 推荐使用开源中文字体，避免中文乱码。

### 4.10 可视化：ECharts

使用 ECharts 作为前端图表工具。

主要图表：

- 分项评分柱状图
- 能力雷达图
- 岗位匹配度图表
- 批量结果对比图

### 4.11 打包部署：Docker Compose + Windows 启动脚本

项目需要同时支持：

- Docker Compose 一键运行
- Windows 本地脚本运行

交付包需要包含：

- `docker-compose.yml`
- 后端 Dockerfile
- 前端 Dockerfile
- Windows 启动脚本
- `.env.example`
- `README.md`
- `database.sql` 或自动建表逻辑

## 5. 系统模块拆分

### 5.1 前端模块

前端建议拆分为以下页面或视图：

- 上传分析页：上传单份 Word/PDF，输入目标岗位，发起分析。
- 批量分析页：上传 ZIP，展示批量分析进度和结果。
- 报告详情页：展示总分、分项分数、图表、问题和建议。
- 历史记录页：查看历史分析列表、下载报告、删除记录。
- 配置页：配置是否启用 DeepSeek 增强分析，展示当前运行模式。

前端需要清晰显示当前模式：

- 离线规则分析
- DeepSeek 增强分析

### 5.2 后端模块

后端建议拆分为以下服务：

- 文件上传服务：保存上传文件，校验类型和大小。
- ZIP 处理服务：安全解压，过滤支持文件。
- 简历解析服务：根据文件类型调用 Word/PDF/OCR 解析。
- OCR 服务：封装 PaddleOCR。
- 评分服务：规则评分、维度评分、总分计算。
- 岗位匹配服务：提取岗位关键词并计算匹配度。
- AI 增强服务：调用 DeepSeek，失败时回退。
- 报告生成服务：生成 Word/PDF 报告。
- 历史记录服务：保存、查询、删除分析记录。
- 数据安全服务：路径校验、文件清理、删除联动。

## 6. 后端接口草案

后端接口统一以 `/api` 开头。

### 6.1 健康检查

`GET /api/health`

用途：检查后端是否启动。

返回示例：

```json
{
  "status": "ok",
  "mode": "deepseek_first",
  "fallback": "offline_rules",
  "database": "mysql"
}
```

### 6.2 单份简历分析

`POST /api/resumes/analyze`

请求类型：`multipart/form-data`

字段：

- `file`：上传的 `.docx` 或 `.pdf`
- `target_position`：目标岗位，可为空
- `job_description`：岗位 JD，可为空
- `enable_ai`：是否优先使用 DeepSeek 分析，默认 `true`

返回内容：

- 分析记录 ID
- 总分
- 分项评分
- 识别模块
- 问题诊断
- 修改建议
- 报告下载 ID 或路径

### 6.3 ZIP 批量分析

`POST /api/resumes/analyze-zip`

请求类型：`multipart/form-data`

字段：

- `file`：上传的 `.zip`
- `target_position`：目标岗位，可为空
- `job_description`：岗位 JD，可为空
- `enable_ai`：是否优先使用 DeepSeek 分析，默认 `true`

返回内容：

- 批量任务 ID
- 总文件数
- 成功数量
- 失败数量
- 每份简历的分析结果摘要
- 汇总报告下载 ID 或路径

### 6.4 历史记录列表

`GET /api/history`

默认返回当前用户的历史记录。第一阶段当前用户固定为 `default_user`。

返回内容：

- 记录 ID
- 文件名
- 目标岗位
- 总分
- 分析时间
- 报告状态

### 6.5 报告下载

`GET /api/reports/{id}/download`

查询参数：

- `format=docx`
- `format=pdf`

返回对应报告文件。

### 6.6 删除历史记录

`DELETE /api/history/{id}`

删除指定分析记录，并同步删除相关文件。

返回示例：

```json
{
  "deleted": true,
  "record_id": 1
}
```

## 7. 数据库表草案

### 7.1 users

用途：预留用户体系。

字段建议：

- `id`
- `username`
- `password_hash`
- `display_name`
- `created_at`

第一阶段默认插入或逻辑使用 `default_user`。

### 7.2 analysis_records

用途：保存单份简历分析记录。

字段建议：

- `id`
- `user_id`
- `batch_task_id`
- `original_filename`
- `target_position`
- `job_description`
- `total_score`
- `scores_json`
- `sections_json`
- `diagnosis_json`
- `suggestions_json`
- `match_result_json`
- `analysis_mode`
- `status`
- `created_at`

### 7.3 batch_tasks

用途：保存 ZIP 批量分析任务。

字段建议：

- `id`
- `user_id`
- `zip_filename`
- `total_files`
- `success_count`
- `failed_count`
- `summary_json`
- `status`
- `created_at`

### 7.4 uploaded_files

用途：保存上传文件和解压文件信息。

字段建议：

- `id`
- `user_id`
- `analysis_record_id`
- `batch_task_id`
- `original_filename`
- `stored_path`
- `file_type`
- `file_size`
- `created_at`

### 7.5 reports

用途：保存报告文件信息。

字段建议：

- `id`
- `user_id`
- `analysis_record_id`
- `batch_task_id`
- `report_type`
- `format`
- `stored_path`
- `created_at`

## 8. 推荐工程目录结构

所有工程文件都放在：

`C:\Users\Patiat\Documents\简历分析`

推荐目录结构：

```text
简历分析/
  README.md
  .env.example
  docker-compose.yml
  database.sql
  start_backend.bat
  start_frontend.bat
  项目功能与技术选型说明.md

  backend/
    app/
      api/
      core/
      models/
      schemas/
      services/
      utils/
    requirements.txt
    Dockerfile

  frontend/
    src/
      api/
      components/
      views/
      router/
      stores/
      charts/
    package.json
    Dockerfile

  data/
    uploads/
    extracted/
    reports/
    samples/

  assets/
    fonts/

  docs/
    使用说明.md
    数据安全说明.md
    演示说明.md
```

## 9. 运行方式要求

### 9.1 Docker 运行

评委或使用者安装 Docker 后，可以通过 Docker Compose 启动项目。

预期命令：

```bash
docker compose up -d
```

启动后访问前端页面，即可上传简历并查看分析报告。

### 9.2 Windows 本地运行

如果不使用 Docker，可以通过 Windows 启动脚本运行。

预期脚本：

- `start_backend.bat`
- `start_frontend.bat`

本地运行前需要安装：

- Python
- Node.js
- MySQL
- PaddleOCR 相关依赖
- 项目 Python 依赖
- 项目前端依赖

具体步骤写入 `README.md`。

## 10. 实现优先级

第一阶段按以下顺序实现：

1. 创建项目目录结构。
2. 创建 FastAPI 后端基础服务和健康检查接口。
3. 创建 Vue 前端基础页面。
4. 实现 MySQL 连接和基础表结构。
5. 实现单份 Word/PDF 上传。
6. 实现 Word/PDF 文本解析。
7. 实现离线规则评分。
8. 实现岗位匹配分析。
9. 实现分析结果页面和 ECharts 图表。
10. 实现 Word/PDF 报告导出。
11. 实现历史记录保存和查看。
12. 实现删除历史记录及文件清理。
13. 实现 ZIP 批量上传和汇总报告。
14. 接入 PaddleOCR 处理扫描 PDF。
15. 接入 DeepSeek 增强分析开关。
16. 最后预留或补充登录相关接口和页面入口。

## 11. 验收标准

最小可运行版本必须满足：

- 可以启动前端、后端和 MySQL。
- 后端 `GET /api/health` 返回正常。
- 前端可以上传 `.docx` 简历。
- 前端可以上传 `.pdf` 简历。
- 前端可以上传 `.zip` 批量分析。
- 无网络时仍能完成完整分析流程。
- 未配置 DeepSeek API Key 时系统不报错。
- 分析结果包含总分和分项评分。
- 分析结果包含问题诊断和修改建议。
- 分析结果包含岗位匹配分析。
- 前端至少展示一种柱状图和一种雷达图。
- 可以导出 Word 报告。
- 可以导出 PDF 报告。
- 历史记录可查看。
- 历史记录可删除。
- 删除历史记录时相关文件被同步清理。
- `.env.example` 不包含真实密钥。
- README 能让别人按步骤运行项目。

## 12. 明确暂缓的内容

以下内容不属于第一阶段必须完成项：

- 完整登录注册流程
- 管理员后台
- 在线部署到公网服务器
- 企业级权限系统
- 多角色管理
- 复杂简历模板生成器
- `.doc` 老格式深度解析

这些功能可以在核心闭环完成后再做。

## 13. 当前已确定结论

本项目最终采用以下方案：

- 前端使用 Vue。
- 后端使用 FastAPI。
- Word 解析使用 python-docx。
- PDF 解析使用 pdfplumber。
- OCR 使用 PaddleOCR。
- AI 使用 DeepSeek API，并保留完整规则兜底。
- 数据处理使用 pandas、jieba、regex。
- 数据库存储使用 MySQL。
- 报告导出使用 python-docx 和 reportlab。
- 图表展示使用 ECharts。
- 部署使用 Docker Compose 和 Windows 本地启动脚本。
- 支持 ZIP 批量上传。
- 支持完整离线可用。
- 默认本地私有存储，不默认外传简历内容。
- 历史记录先使用默认本地用户，后期平滑升级登录。
- 登录功能最后做，当前只预留结构。
